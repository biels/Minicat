package com.biel.lobby.mapes.jocs.roborampage.utils;

/** A deliberately small, full-block palette. Resting scrap never uses fractional geometry. */
public enum ScrapMaterial {
    IRON,
    GOLD,
    COAL,
    GLASS
}
