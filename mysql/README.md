# Historical MySQL development setup

The plugin now uses PostgreSQL exclusively. This directory is retained as a
historical reference; use [PostgreSQL setup](../docs/database/README.md).
Production MySQL recovery artifacts are maintained in `minicat-repo`.
